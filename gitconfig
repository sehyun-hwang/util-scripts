[user]
	name = Sehyun Hwang
	email = hwanghyun3@gmail.com

[core]
	editor = vim

[credential "https://gist.github.com"]
	helper = "${GH} auth git-credential"

[credential "https://github.com"]
	helper = "${GH} auth git-credential"

[filter "lfs"]
	clean = "git-lfs clean -- %f"
	process = "git-lfs filter-process"
	required = true
	smudge = "git-lfs smudge -- %f"

[init]
	defaultBranch = main

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
